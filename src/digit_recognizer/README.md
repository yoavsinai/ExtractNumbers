# Digit Classification

This module implements Stage 4 of the pipeline using a ResNet18 classifier.

## Stage 4: Classification
The `digit_recognizer.py` script handles the final classification of isolated, preprocessed digits (0-9).

## Key Files
- `digit_recognizer.py`: ResNet18 model definition, training loop, and inference logic.
