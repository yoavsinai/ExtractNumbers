# Training Orchestration

Scripts for training the full multi-stage pipeline.

## Key Files
- `train_pipeline.py`: Master orchestrator for training all stages (GlobalBB -> IndividualBB -> Classification). Manages dataset handoffs and model synchronization.
